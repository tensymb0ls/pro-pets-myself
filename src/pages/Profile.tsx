import React from "react";
import { ReduxCounter } from "../components/ReduxCounter/ReduxCounter";

const Profile = () => {
  return (
    <div className="p-[10px] bg-[#fff] rounded-[15px] m-[10px] shadow-componentshadow">
      <ReduxCounter />
    </div>
  );
};

export default Profile;
